# PostgreSQL connection string
DATABASE_URL=postgres://user@localhost:5432/habit_tracker?sslmode=disable

# JWT signing secret — minimal 32 karakter
JWT_SECRET=zenith-habit-tracker-super-secret-key-2026

# Application environment: development | production
APP_ENV=development

# HTTP port (default: 8080)
PORT=8080
